import type { HtmlAttribute } from '../../../utils/web-types';

export default {
    description: 'Insert the html as the next sibling element. This can be used on a `<template>` tag.' +
        'However this isn\'t reactive'
} as HtmlAttribute;
