import { mount } from '@vue/test-utils';
import UIToggle from '@components/toggle/UIToggle.vue';

describe('UIToggle', () => {
    it('should toggle the input with click', () => {
        const wrapper = mount(UIToggle);

    });

    it('should disable input on given prop', () => {
    });
});
